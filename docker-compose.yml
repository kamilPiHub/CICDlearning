services:
  backend-api:
    build: ./backend
    ports:
      - "5000:5000"

  frontend-client:
    build: ./frontend
    environment:
      - BACKEND_URL=http://backend-api:5000/
    depends_on:
      - backend-api